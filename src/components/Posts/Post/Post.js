import React from 'react';
import useStyles from './styles';

const Post = () => {
    const classes = useStyles();
    return (
        <div>
            <h1>post</h1>
        </div>
    );
};

export default Post;